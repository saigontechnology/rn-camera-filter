///
/// HybridBackgroundRendererSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `BackgroundFit` to properly resolve imports.
namespace margelo::nitro::backgroundfilter { enum class BackgroundFit; }
// Forward declaration of `HybridFrameSpec` to properly resolve imports.
namespace margelo::nitro::camera { class HybridFrameSpec; }

#include <string>
#include <optional>
#include "BackgroundFit.hpp"
#include <memory>
#include <VisionCamera/HybridFrameSpec.hpp>
#include <NitroModules/Promise.hpp>

namespace margelo::nitro::backgroundfilter {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `BackgroundRenderer`
   * Inherit this class to create instances of `HybridBackgroundRendererSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridBackgroundRenderer: public HybridBackgroundRendererSpec {
   * public:
   *   HybridBackgroundRenderer(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridBackgroundRendererSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridBackgroundRendererSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridBackgroundRendererSpec() override = default;

    public:
      // Properties
      virtual bool getIsSupported() = 0;
      virtual std::optional<std::string> getUnsupportedReason() = 0;
      virtual bool getIsCaptureSupported() = 0;

    public:
      // Methods
      virtual void setBackground(const std::optional<std::string>& uri, BackgroundFit fit, bool mirror) = 0;
      virtual void setCameraMirrored(bool mirrored) = 0;
      virtual void renderFrame(const std::shared_ptr<margelo::nitro::camera::HybridFrameSpec>& frame) = 0;
      virtual void startCapture(const std::string& outputPath, double maxOutputHeight, double aspectRatio) = 0;
      virtual std::shared_ptr<Promise<std::string>> stopCapture() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "BackgroundRenderer";
  };

} // namespace margelo::nitro::backgroundfilter
