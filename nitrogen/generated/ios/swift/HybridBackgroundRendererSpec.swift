///
/// HybridBackgroundRendererSpec.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import VisionCamera
import NitroModules

/// See ``HybridBackgroundRendererSpec``
public protocol HybridBackgroundRendererSpec_protocol: HybridObject {
  // Properties
  var isSupported: Bool { get }
  var unsupportedReason: String? { get }
  var isCaptureSupported: Bool { get }

  // Methods
  func setBackground(uri: String?, fit: BackgroundFit, mirror: Bool) throws -> Void
  func setCameraMirrored(mirrored: Bool) throws -> Void
  func renderFrame(frame: (any HybridFrameSpec)) throws -> Void
  func startCapture(outputPath: String, maxOutputHeight: Double, aspectRatio: Double) throws -> Void
  func stopCapture() throws -> Promise<String>
}

public extension HybridBackgroundRendererSpec_protocol {
  /// Default implementation of ``HybridObject.toString``
  func toString() -> String {
    return "[HybridObject BackgroundRenderer]"
  }
}

/// See ``HybridBackgroundRendererSpec``
open class HybridBackgroundRendererSpec_base {
  private weak var cxxWrapper: HybridBackgroundRendererSpec_cxx? = nil
  public init() { }
  public func getCxxWrapper() -> HybridBackgroundRendererSpec_cxx {
  #if DEBUG
    guard self is any HybridBackgroundRendererSpec else {
      fatalError("`self` is not a `HybridBackgroundRendererSpec`! Did you accidentally inherit from `HybridBackgroundRendererSpec_base` instead of `HybridBackgroundRendererSpec`?")
    }
  #endif
    if let cxxWrapper = self.cxxWrapper {
      return cxxWrapper
    } else {
      let cxxWrapper = HybridBackgroundRendererSpec_cxx(self as! any HybridBackgroundRendererSpec)
      self.cxxWrapper = cxxWrapper
      return cxxWrapper
    }
  }
}

/**
 * A Swift base-protocol representing the BackgroundRenderer HybridObject.
 * Implement this protocol to create Swift-based instances of BackgroundRenderer.
 * ```swift
 * class HybridBackgroundRenderer : HybridBackgroundRendererSpec {
 *   // ...
 * }
 * ```
 */
public typealias HybridBackgroundRendererSpec = HybridBackgroundRendererSpec_protocol & HybridBackgroundRendererSpec_base
